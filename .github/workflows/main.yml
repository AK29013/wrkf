name: Test Action

on:
  push:
    branches:
      - main  # Trigger on push to the main branch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 23f2001036@ds.study.iitm.ac.in
        run: echo "Hello, world!"


